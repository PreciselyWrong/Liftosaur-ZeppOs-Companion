# Zepp App Store submission

Everything the Zepp OS Developer Console asks for, prepared in one place. No credentials are
needed anywhere: the reviewer runs the application in demo mode, which is what an
installation with no API key does by default.

## Identity

| Field | Value |
| --- | --- |
| App ID | 1123411 |
| App name | Lifto Companion |
| Vendor | Sni3rs |
| Version | 2.0.6, code 20 |
| Category | Health and fitness |
| Privacy policy URL | published `docs/privacy-policy.md` (raw or Pages URL of the public repository) |
| Support | issue tracker of the public repository |

The name deliberately avoids leading with the "Liftosaur" trademark. Liftosaur is named in
the description as the service the application connects to, which is descriptive use, and
the non-affiliation disclaimer is repeated in the listing.

## Short description (en-US)

Track your Liftosaur workouts from your wrist, set by set, with automatic rest timers.

## Full description (en-US)

Lifto Companion is an unofficial, open-source client for Liftosaur, the workout tracking
service. Pick your program, week and day on the watch, log every set as you do it, and let
Liftosaur compute the progression exactly as it would in its own app.

- Your program, your weeks, your days, read live from your Liftosaur account.
- One tap per set, with the prescribed reps and weight on screen.
- Automatic rest timer with a vibration when it ends, including your own default timers for
  standard sets, warmups and supersets.
- Live heart rate during the session.
- Crash proof: the session is written to the watch after every set, so an interrupted
  workout resumes on the same set.
- The finished workout is written straight back to your Liftosaur history, with the
  progression computed by Liftosaur itself.

Try it without an account: with no API key the application runs in demo mode on a sample
program, so you can see exactly how it works before subscribing to anything.

Normal use requires a Liftosaur account with an active Premium subscription and an API key,
which you paste once into the settings page in the Zepp app. The key stays on your phone: it
is never sent to the watch and never written to a log.

This application is independent and is not affiliated with, maintained by, or endorsed by
Liftosaur or its author.

## Translations

### fr-FR

**Courte:** Suivez vos seances Liftosaur au poignet, serie par serie, avec minuteur de repos automatique.

**Longue:**

Lifto Companion est un client non officiel et open source de Liftosaur, le service de suivi
de musculation. Choisissez votre programme, votre semaine et votre jour sur la montre,
enregistrez chaque serie au moment ou vous la faites, et laissez Liftosaur calculer la
progression exactement comme dans son application.

- Vos programmes, vos semaines et vos jours, lus en direct depuis votre compte Liftosaur.
- Une seule pression par serie, avec les repetitions et la charge prevues affichees.
- Minuteur de repos automatique avec vibration a la fin, y compris vos propres durees par defaut pour les series normales, les echauffements et les superseries.
- Frequence cardiaque en direct pendant la seance.
- A l'epreuve des plantages: la seance est ecrite sur la montre apres chaque serie, donc un entrainement interrompu reprend a la meme serie.
- La seance terminee est renvoyee directement dans votre historique Liftosaur, avec la progression calculee par Liftosaur lui-meme.

Essayez sans compte: sans cle API, l'application fonctionne en mode demo sur un programme
d'exemple, ce qui permet de voir exactement comment elle marche avant de s'abonner.

L'usage normal demande un compte Liftosaur avec un abonnement Premium actif et une cle API,
que vous collez une fois dans la page de reglages de l'application Zepp. La cle reste sur
votre telephone: elle n'est jamais envoyee a la montre ni ecrite dans un journal.

Cette application est independante et n'est ni affiliee a Liftosaur ou a son auteur, ni
maintenue ou approuvee par eux.

### de-DE

**Kurz:** Verfolge deine Liftosaur-Workouts am Handgelenk, Satz fur Satz, mit automatischem Pausentimer.

**Lang:**

Lifto Companion ist ein inoffizieller Open-Source-Client fur Liftosaur, den Dienst zur
Trainingsaufzeichnung. Wahle Programm, Woche und Tag auf der Uhr, protokolliere jeden Satz
wahrend du ihn machst, und lass Liftosaur die Progression genau so berechnen wie in seiner
eigenen App.

- Deine Programme, Wochen und Tage, live aus deinem Liftosaur-Konto gelesen.
- Ein Tippen pro Satz, mit den vorgegebenen Wiederholungen und Gewichten auf dem Bildschirm.
- Automatischer Pausentimer mit Vibration am Ende, inklusive deiner eigenen Standardzeiten fur normale Satze, Aufwarmsatze und Supersatze.
- Live-Herzfrequenz wahrend der Einheit.
- Absturzsicher: die Einheit wird nach jedem Satz auf der Uhr gespeichert, ein unterbrochenes Training setzt beim selben Satz fort.
- Das beendete Training wird direkt in deinen Liftosaur-Verlauf zuruckgeschrieben, mit der von Liftosaur selbst berechneten Progression.

Ohne Konto ausprobieren: ohne API-Schlussel lauft die App im Demo-Modus mit einem
Beispielprogramm, sodass du sie vor jedem Abo in Ruhe ansehen kannst.

Fur die normale Nutzung sind ein Liftosaur-Konto mit aktivem Premium-Abo und ein
API-Schlussel notig, den du einmal in die Einstellungsseite in der Zepp-App einfugst. Der
Schlussel bleibt auf deinem Telefon: er wird nie an die Uhr gesendet und nie in ein
Protokoll geschrieben.

Diese App ist unabhangig und steht in keiner Verbindung zu Liftosaur oder dessen Autor, wird
von ihnen weder gepflegt noch unterstutzt.

### es-ES

**Corta:** Registra tus entrenamientos de Liftosaur desde la muneca, serie a serie, con temporizador de descanso automatico.

**Larga:**

Lifto Companion es un cliente no oficial y de codigo abierto para Liftosaur, el servicio de
seguimiento de entrenamientos. Elige tu programa, tu semana y tu dia en el reloj, registra
cada serie mientras la haces y deja que Liftosaur calcule la progresion igual que en su
propia aplicacion.

- Tus programas, semanas y dias, leidos en directo desde tu cuenta de Liftosaur.
- Un toque por serie, con las repeticiones y el peso prescritos en pantalla.
- Temporizador de descanso automatico con vibracion al terminar, incluidos tus propios tiempos por defecto para series normales, calentamientos y superseries.
- Frecuencia cardiaca en directo durante la sesion.
- A prueba de fallos: la sesion se guarda en el reloj despues de cada serie, asi que un entrenamiento interrumpido continua en la misma serie.
- El entrenamiento terminado se escribe directamente en tu historial de Liftosaur, con la progresion calculada por el propio Liftosaur.

Pruebalo sin cuenta: sin clave de API la aplicacion funciona en modo demo con un programa de
ejemplo, para que veas exactamente como funciona antes de suscribirte a nada.

El uso normal requiere una cuenta de Liftosaur con una suscripcion Premium activa y una
clave de API, que pegas una sola vez en la pagina de ajustes de la aplicacion Zepp. La clave
se queda en tu telefono: nunca se envia al reloj ni se escribe en un registro.

Esta aplicacion es independiente y no esta afiliada a Liftosaur ni a su autor, ni mantenida
ni respaldada por ellos.

### it-IT

**Breve:** Registra i tuoi allenamenti Liftosaur dal polso, serie dopo serie, con timer di recupero automatico.

**Lunga:**

Lifto Companion e un client non ufficiale e open source per Liftosaur, il servizio di
tracciamento degli allenamenti. Scegli programma, settimana e giorno sull'orologio, registra
ogni serie mentre la esegui e lascia che Liftosaur calcoli la progressione esattamente come
nella sua app.

- I tuoi programmi, le tue settimane e i tuoi giorni, letti in tempo reale dal tuo account Liftosaur.
- Un tocco per serie, con ripetizioni e carico prescritti a schermo.
- Timer di recupero automatico con vibrazione alla fine, inclusi i tuoi tempi predefiniti per serie normali, riscaldamento e superserie.
- Frequenza cardiaca in tempo reale durante la sessione.
- A prova di crash: la sessione viene salvata sull'orologio dopo ogni serie, quindi un allenamento interrotto riprende dalla stessa serie.
- L'allenamento concluso viene scritto direttamente nella tua cronologia Liftosaur, con la progressione calcolata da Liftosaur stesso.

Provalo senza account: senza chiave API l'applicazione funziona in modalita demo su un
programma di esempio, cosi puoi vedere esattamente come funziona prima di sottoscrivere
qualsiasi cosa.

L'uso normale richiede un account Liftosaur con un abbonamento Premium attivo e una chiave
API, che incolli una volta nella pagina delle impostazioni nell'app Zepp. La chiave resta
sul telefono: non viene mai inviata all'orologio ne scritta in un log.

Questa applicazione e indipendente e non e affiliata a Liftosaur o al suo autore, ne
mantenuta o approvata da loro.

### zh-CN

**简介:** 在手腕上逐组记录你的 Liftosaur 训练，并自动计时组间休息。

**详细介绍:**

Lifto Companion 是 Liftosaur 训练记录服务的非官方开源客户端。在手表上选择你的训练计划、
周次和训练日，边练边记录每一组，进阶计算完全交给 Liftosaur，与其官方应用完全一致。

- 你的训练计划、周次和训练日，实时从你的 Liftosaur 账户读取。
- 每组只需轻点一次，屏幕上显示计划的次数和重量。
- 自动组间休息计时，结束时震动提醒，并支持你自己设定的常规组、热身组和超级组默认时长。
- 训练过程中实时显示心率。
- 防崩溃：每完成一组都会写入手表存储，训练被中断后可从同一组继续。
- 训练结束后直接写回你的 Liftosaur 历史记录，进阶由 Liftosaur 自身计算。

无需账户即可试用：未填写 API 密钥时，应用会以示例训练计划运行演示模式，让你在订阅之前
先完整体验。

正常使用需要一个已开通 Premium 订阅的 Liftosaur 账户和一个 API 密钥，只需在 Zepp 应用的
设置页面粘贴一次。密钥仅保存在手机上：绝不会发送到手表，也绝不会写入日志。

本应用为独立开发，与 Liftosaur 及其作者无隶属关系，亦未获其维护或认可。

Wording note: the disclaimer and the Premium requirement are present in all six locales.
They are the two sentences a reviewer looks for, so they are never dropped in translation.

## Assets

| Asset | State |
| --- | --- |
| Icon (`icon.png`, round and square variants) | present in `assets/common.r` and `assets/square.s` |
| Store screenshots | **TODO**, capture from a real Amazfit Active 2 and an Amazfit Bip 6 |

Screenshots to capture, in this order, because they tell the story a reviewer needs:

1. Program picker
2. Week and day picker
3. Active set screen with the prescription visible
4. Rest timer running
5. Session finished with the summary

The console imposes its own dimensions and count per screen shape. Read the form before
capturing, `docs/screenshots/` holds the working copies.

## Notes for the reviewer

Paste this into the review notes field. It needs no credentials.

> **No account is needed to review this application.**
>
> It is a client for the third party service Liftosaur (https://www.liftosaur.com), but an
> installation with no API key runs in **demo mode**: it serves a built-in sample program
> and lets you run a complete workout, so the whole application can be reviewed without
> signing up for anything.
>
> To review: install, open the application on the watch, and use it. No setup at all. The
> settings page in the Zepp app shows "Status: Demo mode" to confirm it. Nothing is sent to
> any server in this mode, and nothing is saved to any account.
>
> A real Liftosaur account with an active Premium subscription is required only for the
> normal use of the application, because the public Liftosaur API is a Premium feature.
> This is stated in the description.
>
> Privacy: the API key, when a user enters one, is stored only in the phone side settings
> storage and is sent only to https://www.liftosaur.com as an Authorization header. It is
> never transmitted to the watch. Heart rate is displayed on the watch during a session and
> is never transmitted anywhere. Full details: <privacy policy URL>.

## Demo mode

Implemented by [`app-side/dummy-program-service.js`](../app-side/dummy-program-service.js)
and selected in [`app-side/index.js`](../app-side/index.js). It engages when the stored API
key is empty, or is literally `dummy` or `demo`. It serves three sample programs, a full
day plan and a fake finish response, and performs no HTTP request at all.

This is what a reviewer, and any tester without a Premium subscription, sees. It is also
what the store screenshots are captured from, so no personal workout data ends up in the
listing.

Before submitting, run one full demo session end to end on a real watch: pick a program,
pick a week and a day, complete every set, finish. The reviewer's first run must not be the
first run ever.

## Submission checklist

- [ ] Demo mode run end to end on a real watch, no key stored
- [ ] Privacy policy published at a public URL
- [ ] Screenshots captured for every targeted screen shape
- [x] Descriptions written for every declared locale
- [ ] `version.code` higher than the last submitted version
- [ ] `npm test` green
- [ ] `/public-release-audit` passed, no credentials in the repository
- [ ] `zeus build` run, the `.zab` in `dist/` matches the declared version
